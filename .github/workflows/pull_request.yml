---
name: Pull Request
on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # holden:ignore:HLD_CI_017: reusable workflow lives in our own org's .github repo, intentionally tracked at main
  pr:
    uses: JamesWoolfenden/.github/.github/workflows/terraform-pr.yml@main
